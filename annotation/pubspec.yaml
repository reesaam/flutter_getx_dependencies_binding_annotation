name: getx_binding_annotation
description: A Code Generator Plugin to Generate Pages and Dependencies for GetX State Manager base on Annotation.
version: 0.4.1
homepage: https://resam.site
repository: https://github.com/reesaam/flutter_getx_dependencies_binding_annotation.git
funding:
  - https://resam.site

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=3.11.0 <4.0.0"

features:
  dot-shorthands: true

dependencies:
  build_runner: ^2.15.1
#  dartdoc: ^9.0.8

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0

dependency_overrides:
  meta: 1.17.0